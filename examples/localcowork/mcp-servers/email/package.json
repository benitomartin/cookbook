{
  "name": "@localcowork/mcp-email",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server — MBOX/Maildir parsing and SMTP sending",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/ --ext .ts"
  },
  "dependencies": {
    "@localcowork/mcp-shared": "*",
    "zod": "^3.22.0",
    "better-sqlite3": "^11.0.0",
    "mailparser": "^3.6.0",
    "nodemailer": "^6.9.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.7.0",
    "@types/node": "^20.11.0",
    "@types/better-sqlite3": "^7.6.0",
    "@types/nodemailer": "^6.4.0",
    "vitest": "^1.4.0"
  }
}
